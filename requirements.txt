# Python 3.12 -- torch has no wheels for the 3.14 currently on this machine.
# Install torch FIRST, from the CUDA index, then the rest:
#   pip install torch --index-url https://download.pytorch.org/whl/cu126
torch>=2.4
numpy>=1.26
opencv-python-headless>=4.9
PyYAML>=6.0
matplotlib>=3.8
tqdm>=4.66

# reference-only: needed to import the official implementations in reference/
# for tests/test_reference_equivalence.py. NOT imported by datnet/ itself.
# Note: timm pulls torchvision (CPU build); harmless, nothing here uses it.
einops>=0.8
timm>=1.0
